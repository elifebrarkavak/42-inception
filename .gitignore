# Secrets — never commit real credentials
secrets/*.txt

# .env keeps non-sensitive config (per subject); if a real password
# is ever put in one, ignore it instead of pushing it
**/.env.local

# macOS
.DS_Store
